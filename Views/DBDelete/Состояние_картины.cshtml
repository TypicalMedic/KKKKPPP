@model DBDeleteViewModel
<div class="row mt-5 mb-2">
    <h4>Удаление информации о состоянии картины</h4>
    @if (Model.ids.Length == 0)
    {
        <h4>Выберете состояние/ состояния</h4>
        <form method="get" action="/DBDelete/Состояние_картины" name="Form">
            <select class="form-select form-select-sm mb-3 mt-3" multiple required name="id" size="10">
                @foreach (var x in Model.allCondit)
                {
                    if (Model.ids.Contains(x.Код_состояния.ToString()))
                    {
                        <option selected value="@x.Код_состояния">@x.Код_состояния @x.Состояние</option>
                    }
                    else
                    {
                        <option value="@x.Код_состояния">@x.Код_состояния @x.Состояние</option>
                    }
                }
            </select>
            <p>
                <button name="clear" value="false">Продолжить</button>
                <button name="clear" value="true">Сбросить выбор</button>
            </p>
        </form>
    }
    else
    {
        <form method="get" action="/DBDelete/Состояние_картины" name="Form">
            <p>
                <button name="clear" value="true">Сбросить выбор</button>
            </p>
        </form>
        <h4>Информация о выбранных жанрах:</h4>
        <p>
            <table border="1" align="center"
                   cellpadding="4" cellspacing="0">
                <tr value="">
                    @*@foreach (var x in entPropts)
                        {
                            <th style="border:1px solid black;">@x</th>
                        }*@
                    <th style="border:1px solid black;">Код</th>
                    <th style="border:1px solid black;">Название</th>
                </tr>
                @foreach (var x in Model.allCondit.Where(p => Model.ids.Contains(p.Код_состояния.ToString())))
                {
                    <tr value="@x.Код_состояния">
                        <td rowspan="4" style="border:1px solid black;">@x.Код_состояния</td>
                        <td style="border:1px solid black;"> @x.Состояние</td>
                    </tr>
                    <tr><th style="border:1px solid black;" class="text-center" colspan="14">Будут также заменены на стандартное значение следующие связанные с данным состоянием записи</th></tr>
                    <tr><td style="border:1px solid black;" colspan="14">Запись об этом состоянии в следующих картинах:</td></tr>
                    <tr>
                        @{ string pics = "";}
                        @foreach (var y in Model.allPictures.Where(s => s.Состояние == x.Код_состояния))
                        {
                            pics += y.Инвентарный_номер + " (" + y.Название + ")           ";
                            @*<td style="border:1px solid black;" class="text-lg-start">@y.Инвентарный_номер (@y.Название)</td>*@
                        }
                        <td>@pics</td>
                    </tr>
                }
            </table>
            <div>

            </div>
        </p>
        <form method="post" action="/DBDelete/Delete">
            <input hidden name="type" value="Состояние_картины" />
            @foreach (var x in Model.ids)
            {
                <input hidden name="id" value="@x" />
            }
            <button name="ids" value="@Model.ids">Удалить</button>
        </form>
    }
</div>
