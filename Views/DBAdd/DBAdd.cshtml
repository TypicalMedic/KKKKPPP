@model DBAddViewModel
<h1 class="row mt-3">Добавление в БД</h1>
<div class="row mb-2">
    <form class="row mt-2 mb-2">
        <div>
            <h5 id="1">Какой элемент добавить</h5>
            @*<select id="Obj-type">
                <option value="1">Картина</option>
                <option value="2">Автор</option>
                <option value="3">Страна</option>
            </select>*@
        </div>
        @*<p><input type="submit" value="Отправить"></p>*@

        @foreach (var x in Model.allEntities.Where(e => !e.Contains("Связь_")))
        {<div class="col-lg-3">
                <a class="nav-link active" aria-current="page" href="/DBAdd/@x">@x.Replace('_', ' ')</a>
            </div>
        }
        @*<a class="nav-link active" aria-current="page" href="/DBAdd/Картина">Добавить картину</a>*@
    </form>
</div>